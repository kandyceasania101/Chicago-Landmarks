package com.example.kandyceasania.project3_a2;

//sends updates on the current index so that all fragments are aware of the change

public interface ListSelectionListener {
    void onListSelection(int index);
}
